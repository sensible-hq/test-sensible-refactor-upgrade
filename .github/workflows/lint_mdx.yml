name: Lint MDX Files

on: [push, pull_request]

jobs:
  lint:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v2

    - name: Lint MDX files
      uses: tinovyatkin/md-lint-action@v3
      with:
        files: '**/*.mdx'
